services:

  app:
    build: .

  db:
    build:
      context: .
      dockerfile: Dockerfile_dataset
    restart: always
    ports:
      - "33060:3306"
    environment:
      MYSQL_ROOT_PASSWORD: example